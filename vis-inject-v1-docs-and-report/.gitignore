# Local Python environment for report scripts.
.venv/

# Slide deck is built locally — don't track binary .pptx.
slides/*.pptx

# … but the curated copy under submission/ IS tracked.
!submission/*.pptx

# Math equation PNGs rendered by build_slides.py at runtime.
slides/_math/

# OS noise
.DS_Store
